MEMORY {
    # Full 64K image - test harness loads at $0000
    RAM: start = $0000, size = $10000, type = rw, file = %O, fill = yes;
}

SEGMENTS {
    ZEROPAGE: load = RAM, type = zp, start = $0000;
    CODE:     load = RAM, type = ro, start = $0200;
}
