[package]
name = "zilog-z80"
description = "Z80 CPU — half-cycle signal-level state machine"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
