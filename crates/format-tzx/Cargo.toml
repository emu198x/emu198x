[package]
name = "format-tzx"
description = "TZX/CDT block parsing, shared by every system that reads the format"
edition.workspace = true
version.workspace = true
publish = false
license.workspace = true
repository.workspace = true

[dependencies]
common-tape = { path = "../common-tape", version = "0.13.0" }
format198x-tzx = "0.1"

[lints]
workspace = true
