[package]
name = "peripheral-commodore-amiga-floppy"
description = "Amiga floppy drive mechanism — head positioning, motor control, disk change detection, MFM track encoding."
edition.workspace = true
version.workspace = true
publish = false
license.workspace = true
repository.workspace = true

[dependencies]
format-commodore-amiga-adf = { path = "../format-commodore-amiga-adf", version = "0.5.0" }
serde = { workspace = true }

[lints]
workspace = true
