name: Nightly Accuracy

# Runs the external CPU conformance corpora that are too large / too slow to
# gate every push — the Tom Harte single-step suites (6502, Z80, 68000), SM83
# (Tennant), Klaus Dormann, FUSE Z80, and Wolfgang Lorenz. These tests are
# `#[ignore]`d by default (they need corpora that don't live in the repo), so
# without this workflow the project's strongest validation runs only on a dev
# machine by hand. This is the standing gate that makes the accuracy claims real.
#
# Corpora come from a private "dedicated assets store" (a GitHub repo's release
# assets) that you control — see test-data/accuracy-corpora.md. That keeps them
# hermetic and lets the Lorenz tarball carry the C64 KERNAL without any public
# ROM redistribution. ZEXDOC/ZEXALL is hermetic and lives in zex.yml, not here.

on:
  schedule:
    # 02:30 UTC nightly — off-peak for GitHub-hosted runners.
    - cron: '30 2 * * *'
  workflow_dispatch:

concurrency:
  group: nightly-accuracy-${{ github.ref }}
  cancel-in-progress: true

env:
  # The dedicated assets store. Set ACCURACY_CORPORA_REPO as an Actions
  # variable (e.g. emu198x/accuracy-corpora); ACCURACY_CORPORA_TAG defaults
  # to v1. The read token is the ACCURACY_CORPORA_TOKEN secret.
  CORPORA_REPO: ${{ vars.ACCURACY_CORPORA_REPO }}
  CORPORA_TAG: ${{ vars.ACCURACY_CORPORA_TAG || 'v1' }}

jobs:
  # Secrets can't be read in a job-level `if`, so a preflight job turns
  # "is the store configured?" into an output the corpus jobs gate on. On a
  # fork or a PR without the secret this short-circuits everything to skipped
  # (not failed), keeping those runs green.
  preflight:
    name: Preflight
    runs-on: ubuntu-latest
    outputs:
      configured: ${{ steps.check.outputs.configured }}
    steps:
      - name: Check corpora store is configured
        id: check
        run: |
          if [ -n "${{ secrets.ACCURACY_CORPORA_TOKEN }}" ] && [ -n "${CORPORA_REPO}" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
            echo "Corpora store: ${CORPORA_REPO}@${CORPORA_TAG}"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Corpora store not configured (ACCURACY_CORPORA_TOKEN secret and/or ACCURACY_CORPORA_REPO variable unset). Corpus jobs skipped. See test-data/accuracy-corpora.md."
          fi

  # Fast-to-moderate corpora, one matrix leg each. Each leg pulls its tarball
  # from the store, verifies it against the store's SHA256SUMS, extracts it,
  # points the corpus env var at the extracted dir, and runs that test file
  # with --include-ignored (the whole ignored set for the file, matching the
  # zex.yml precedent).
  corpora:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    needs: preflight
    if: needs.preflight.outputs.configured == 'true'
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: harte-6502
            artifact: harte-6502
            crate: mos-6502
            test: single_step_tests
            env_var: EMU198X_6502_TOM_HARTE_DIR
          - name: harte-z80
            artifact: harte-z80
            crate: zilog-z80
            test: single_step_tests
            env_var: EMU198X_Z80_TOM_HARTE_DIR
          - name: sm83-tennant
            artifact: sm83
            crate: sharp-lr35902
            test: single_step_tests
            env_var: EMU198X_SM83_TENNANT_DIR
          - name: dormann-6502
            artifact: dormann-6502
            crate: mos-6502
            test: dormann_tests
            env_var: EMU198X_6502_DORMANN_DIR
          - name: fuse-z80
            artifact: fuse-z80
            crate: zilog-z80
            test: z80_fuse
            env_var: EMU198X_FUSE_Z80_TESTS_DIR
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Read pinned toolchain
        id: toolchain
        run: |
          channel=$(awk -F'"' '/^channel/{print $2}' rust-toolchain.toml)
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: nightly-accuracy-${{ matrix.crate }}

      - name: Provision corpus from store
        env:
          GH_TOKEN: ${{ secrets.ACCURACY_CORPORA_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p corpora
          cd corpora
          gh release download "${CORPORA_TAG}" -R "${CORPORA_REPO}" \
            -p '${{ matrix.artifact }}.tar.zst' -p 'SHA256SUMS'
          # Verify only the asset we pulled against the store's checksums.
          grep ' ${{ matrix.artifact }}.tar.zst$' SHA256SUMS | sha256sum -c -
          tar --zstd -xf '${{ matrix.artifact }}.tar.zst'
          echo "${{ matrix.env_var }}=$(pwd)/${{ matrix.artifact }}" >> "$GITHUB_ENV"

      - name: Run ${{ matrix.name }}
        run: cargo test --release -p ${{ matrix.crate }} --test ${{ matrix.test }} -- --include-ignored --nocapture

  # Tom Harte 68000 — the ~1,000,058-case sweep. Gigabytes and potentially
  # hours, so it gets its own job with a long timeout rather than sharing the
  # matrix. This is the "million-test" claim's standing gate.
  harte-68000:
    name: harte-68000
    runs-on: ubuntu-latest
    needs: preflight
    if: needs.preflight.outputs.configured == 'true'
    timeout-minutes: 360
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Read pinned toolchain
        id: toolchain
        run: |
          channel=$(awk -F'"' '/^channel/{print $2}' rust-toolchain.toml)
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: nightly-accuracy-motorola-68000

      - name: Provision corpus from store
        env:
          GH_TOKEN: ${{ secrets.ACCURACY_CORPORA_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p corpora
          cd corpora
          gh release download "${CORPORA_TAG}" -R "${CORPORA_REPO}" \
            -p 'harte-68000.tar.zst' -p 'SHA256SUMS'
          grep ' harte-68000.tar.zst$' SHA256SUMS | sha256sum -c -
          tar --zstd -xf 'harte-68000.tar.zst'
          echo "EMU198X_68000_TOM_HARTE_ROOT=$(pwd)/harte-68000" >> "$GITHUB_ENV"

      - name: Run Tom Harte 68000 full sweep
        run: cargo test --release -p motorola-68000 --test tom_harte -- --include-ignored --nocapture

  # Wolfgang Lorenz — the only corpus here that also needs the C64 KERNAL, so
  # the store's lorenz-6502 tarball must include kernal.rom. Kept as its own
  # job to make the firmware dependency explicit rather than hidden in the
  # matrix.
  #
  # DEFERRED (2026-07-04): the Lorenz suite isn't in the store yet — it needs
  # the suite obtained and a decision on carrying the C64 KERNAL in the private
  # tarball. Disabled with `if: false` so the nightly stays green rather than
  # red on a missing asset. To re-enable once lorenz-6502.tar.zst is uploaded,
  # restore the condition to: needs.preflight.outputs.configured == 'true'.
  lorenz-6502:
    name: lorenz-6502
    runs-on: ubuntu-latest
    needs: preflight
    if: false
    timeout-minutes: 120
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Read pinned toolchain
        id: toolchain
        run: |
          channel=$(awk -F'"' '/^channel/{print $2}' rust-toolchain.toml)
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.toolchain.outputs.channel }}

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: nightly-accuracy-lorenz

      - name: Provision corpus + KERNAL from store
        env:
          GH_TOKEN: ${{ secrets.ACCURACY_CORPORA_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p corpora
          cd corpora
          gh release download "${CORPORA_TAG}" -R "${CORPORA_REPO}" \
            -p 'lorenz-6502.tar.zst' -p 'SHA256SUMS'
          grep ' lorenz-6502.tar.zst$' SHA256SUMS | sha256sum -c -
          tar --zstd -xf 'lorenz-6502.tar.zst'
          echo "EMU198X_6502_LORENZ_DIR=$(pwd)/lorenz-6502" >> "$GITHUB_ENV"
          # The suite reads the KERNAL via the C64 ROM env var; the tarball
          # ships it alongside the .prg cases.
          echo "EMU198X_C64_KERNAL_ROM=$(pwd)/lorenz-6502/kernal.rom" >> "$GITHUB_ENV"

      - name: Run Wolfgang Lorenz 6502 suite
        run: cargo test --release -p mos-6502 --test lorenz_tests -- --include-ignored --nocapture
